# UI manifest — the DOM wire contract (version 1)

Status: current (stage 4 of [TUI_DESIGN.md](TUI_DESIGN.md), #1141 part 2). The
manifest is the serialization boundary between Finch's widget engine and a
future GUI client (the Tauri consumer of #808). The maintainer's ruling: one
ViewModel, two lowering methods, no fork — the same tree the terminal lowers
to styled cells lowers to this manifest. Components never write HTML; the
engine owns the lowering, and a component's `render_dom` remains an unused
override hook.

## Envelope

The wire payload is one JSON object; a consumer reads `manifest_version`
first and rejects (or versions) everything else:

```json
{ "manifest_version": 1, "root": { …DynamicUiNode… } }
```

| Version | When | Notes |
|---|---|---|
| 1 | 2026-09 (this document) | initial contract |

Any shape change bumps the version and updates this document and the TS
artifacts in the same commit.

## Node shape

```json
{
  "element_type": "SayTurnCard",
  "id": "7c9e6679-7425-40de-944b-e07fc1f90ae7",
  "props": { "status": "completed", "elapsedMs": 2350, "showProgram": false },
  "children": [ … ]
}
```

- `element_type` — the registry key a JSX-side component registry maps to a
  component (Vite glob imports keep it decentralized, per the design doc).
- `id` — **derived** identity, never minted at lowering time: a message's
  uuid, or `{uuid}#{path}` for a semantic path under it (path segments
  joined by dots; `[1]` → `{uuid}#1`). This matches the terminal renderer's
  `RowId` discipline, so `propagate_ui_action(widget_id, action_id, payload)`
  routes back through the same ids.
- `props` — JSON-valued, sorted keys (the Rust side uses a `BTreeMap`), never
  pre-rendered presentation strings. A GUI component reads semantics and
  decides its own presentation.
- `children` — ordered.

### Styled text on the wire

A rendered line's spans lower to `ManifestSpan[]`:
`{ text, fg?, bg?, bold?, dim? }`, colours `{ "Indexed": 0–15 }` or
`{ "Rgb": [r, g, b] }`. Absent optional fields are omitted; a plain span
serializes as `{"text": "…"}`.

## Element types and props (contract v1)

| element_type | props | children |
|---|---|---|
| `SayTurnCard` | `status` (`running`/`completed`), `elapsedMs`, `showProgram` | `ProgramSource` (`#0`), `Output` (`#1`, present when output exists) |
| `ProgramSource` | `language`, `lines` | — |
| `Output` | `lines` | — |
| `StaticText` | `kind` (`info`/`error`/`success`/`warning`/`plain`), `lines` | — |
| `Progress` | `label`, `current`, `total`, `status` (`in_progress`/`complete`/`failed`) | — |
| `LiveTool` | `header`, `lines`, `status` | — |
| `Operation` | `header`, `status` | `OperationRow` |
| `OperationRow` | `label`, `status` (`{state}`, plus `summary`/`detail` when present) | — |

The engine vocabulary lowers with these element types: `Stack` (`axis`,
per-child `track`), `Text` (`lines`), `Viewport` (`Line` children carrying
`text`, `spans`, and hit metadata), `DialogCard` (`lines`), `Rule`,
`Completions` (`rows`), `Composer` (`inputLines`, `ghost`).

## Backpropagation

Events return from the DOM as the same opaque `(RowId, Action)` pairs the
terminal routes: one generic IPC command
`propagate_ui_action(widget_id, action_id, payload)` routes to the component
by id. Transport is #808's scope; this document pins only the payload shape.

## Artifacts and proof

- Rust source of truth: `crates/finch-tui/src/dom_manifest.rs`
- Generated TS types (committed, regenerated by `cargo test -p finch-tui
  --lib` via ts-rs): `crates/finch-tui/bindings/dom/`
- Golden snapshot: `crates/finch-tui/tests/fixtures/ui_manifest_say_card.json`,
  pinned by `crates/finch-tui/tests/ui_manifest.rs` (regenerate deliberately
  with `FINCH_UPDATE_UI_MANIFEST_GOLDEN=1` and commit the diff with this
  document's version bump).

## JSON schema (v1)

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://finch.dev/schemas/ui-manifest-v1.json",
  "title": "Finch UI manifest",
  "type": "object",
  "required": ["manifest_version", "root"],
  "properties": {
    "manifest_version": { "const": 1 },
    "root": { "$ref": "#/$defs/node" }
  },
  "$defs": {
    "color": {
      "oneOf": [
        { "type": "object", "required": ["Indexed"], "properties": { "Indexed": { "type": "integer", "minimum": 0, "maximum": 15 } }, "additionalProperties": false },
        { "type": "object", "required": ["Rgb"], "properties": { "Rgb": { "type": "array", "items": { "type": "integer", "minimum": 0, "maximum": 255 }, "minItems": 3, "maxItems": 3 } }, "additionalProperties": false }
      ]
    },
    "span": {
      "type": "object",
      "required": ["text"],
      "properties": {
        "text": { "type": "string" },
        "fg": { "$ref": "#/$defs/color" },
        "bg": { "$ref": "#/$defs/color" },
        "bold": { "type": "boolean" },
        "dim": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "node": {
      "type": "object",
      "required": ["element_type", "id", "props", "children"],
      "properties": {
        "element_type": { "type": "string" },
        "id": { "type": "string" },
        "props": { "type": "object" },
        "children": { "type": "array", "items": { "$ref": "#/$defs/node" } }
      },
      "additionalProperties": false
    }
  }
}
```